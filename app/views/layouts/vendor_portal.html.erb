<!DOCTYPE html>
<html>
<head>
  <title>Vendor Portal - MAT Vulcan</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body class="min-h-screen bg-gray-50">
  <header class="bg-blue-900 text-white py-4">
    <div class="container mx-auto px-4">
      <div class="flex justify-between items-center">
        <div class="flex items-center">
          <div class="text-xl font-bold">MAT Vulcan Vendor Portal</div>
        </div>
        <% if current_user %>
          <div class="flex items-center space-x-4">
            <div><%= current_user.name %></div>
            <div><%= link_to "Profile", edit_vendor_portal_profile_path, class: "text-gray-200 hover:text-white" %></div>
            <div><%= link_to "Dashboard", vendor_portal_dashboard_path, class: "text-gray-200 hover:text-white" %></div>
            <div><%= link_to "Vouchers", vendor_portal_vouchers_path, class: "text-gray-200 hover:text-white" %></div>
            <div><%= link_to "Transactions", vendor_portal_transactions_path, class: "text-gray-200 hover:text-white" %></div>
            <div><%= button_to "Sign Out", session_path, method: :delete, class: "text-gray-200 hover:text-white" %></div>
          </div>
        <% end %>
      </div>
    </div>
  </header>

  <main class="container mx-auto px-4 py-6">
    <%= render "layouts/flash" if flash.any? %>
    <%= yield %>
  </main>

  <footer class="bg-gray-200 py-4 mt-8">
    <div class="container mx-auto px-4 text-center text-gray-600">
      &copy; <%= Date.current.year %> MAT Vulcan - All rights reserved
    </div>
  </footer>
</body>
</html>
