<div class="space-y-4">
  <!-- Income Proof Section -->
  <div class="flex items-center justify-between">
    <div class="flex items-center space-x-2">
      <svg class="h-5 w-5 text-gray-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
      <span class="text-sm text-gray-900">
        Income Proof:
        <span class="px-2 py-1 text-sm font-medium rounded-full <%= badge_class_for(:proof, @application.income_proof_status) %>">
          <%= @application.income_proof_status.titleize %>
        </span>
      </span>
    </div>
    <div>
      <% if @application.income_proof.attached? %>
        <% if @application.income_proof_status == "approved" %>
          <%= link_to rails_blob_path(@application.income_proof, disposition: :inline),
              target: "_blank",
              rel: "noopener noreferrer",
              class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none",
              data: { turbo: false } do %>
            <svg class="h-5 w-5 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            View Proof
          <% end %>
        <% else %>
          <button type="button"
                  data-action="click->modal#open"
                  data-modal-id="incomeProofReviewModal"
                  data-proof-type="income"
                  class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none">
            Review Proof
          </button>
        <% end %>
      <% else %>
        <span class="text-sm text-gray-500">Not Attached</span>
      <% end %>
    </div>
  </div>
  <!-- Residency Proof Section -->
  <div class="flex items-center justify-between">
    <div class="flex items-center space-x-2">
      <svg class="h-5 w-5 text-gray-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
      <span class="text-sm text-gray-900">
        Residency Proof:
        <span class="px-2 py-1 text-sm font-medium rounded-full <%= badge_class_for(:proof, @application.residency_proof_status) %>">
          <%= @application.residency_proof_status.titleize %>
        </span>
      </span>
    </div>
    <div>
      <% if @application.residency_proof.attached? %>
        <% if @application.residency_proof_status == "approved" %>
          <%= link_to rails_blob_path(@application.residency_proof, disposition: :inline),
              target: "_blank",
              rel: "noopener noreferrer",
              class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none",
              data: { turbo: false } do %>
            <svg class="h-5 w-5 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            View Proof
          <% end %>
        <% else %>
          <button type="button"
                  data-action="click->modal#open"
                  data-modal-id="residencyProofReviewModal"
                  data-proof-type="residency"
                  class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none">
            Review Proof
          </button>
        <% end %>
      <% else %>
        <span class="text-sm text-gray-500">Not Attached</span>
      <% end %>
    </div>
  </div>
  <!-- Medical Certification Section -->
  <div class="flex items-center justify-between">
    <div class="flex items-center space-x-2">
      <svg class="h-5 w-5 text-gray-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
      <span class="text-sm text-gray-900">
        Medical Certification:
        <span class="px-2 py-1 text-sm font-medium rounded-full <%= badge_class_for(:proof, @application.medical_certification_status) %>">
          <%= @application.medical_certification_status.titleize %>
        </span>
      </span>
    </div>
    <div>
      <% if @application.medical_certification.attached? %>
        <% if @application.medical_certification_status_accepted? %>
          <%= link_to rails_blob_path(@application.medical_certification, disposition: :inline),
              target: "_blank",
              rel: "noopener noreferrer",
              class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none",
              data: { turbo: false } do %>
            <svg class="h-5 w-5 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            View Certification
          <% end %>
        <% else %>
          <button type="button"
                  data-action="click->modal#open"
                  data-modal-id="medicalCertificationReviewModal"
                  class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none">
            Review Certification
          </button>
        <% end %>
      <% else %>
        <span class="text-sm text-gray-500">Not Attached</span>
      <% end %>
    </div>
  </div>
</div>
